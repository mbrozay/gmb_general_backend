'use strict';

// Register `epList` component, along with its associated controller and template
angular.
  module('epEdit').
  component('epEdit' ,{
	  templateUrl: 'ep-edit/ep-edit.template.html',

	  controller: [function EPLoginController(EpLogin) {
		  var ctrl = this;
		  
		  
	  }
	  ]
	  
	});